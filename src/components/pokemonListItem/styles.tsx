import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  tile: {
    marginLeft: 10,
    alignItems: "center",
    flexDirection: "row",
  },
  right: {
    alignItems: "center",
    justifyContent: "center",
    padding: 10,
    borderRadius: 50,
  },
});
